source 'http://rubygems.org'

gem 'rails', '3.0.0'
gem 'jquery-rails', '>= 0.2.6'
gem 'mechanize'
gem 'mongoid'
gem 'redis'

group :deployment do
	gem 'capistrano'
	gem 'git-deploy'
end

group :development, :test do
	gem 'ruby-debug'
end
